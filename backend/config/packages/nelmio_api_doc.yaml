nelmio_api_doc:
    documentation:
        info:
            title: Product Import API
            description: REST API for importing products from XLSX and browsing imported products.
            version: 1.0.0
    html_config:
        assets_mode: offline
        swagger_ui_config:
            deepLinking: true
    areas:
        path_patterns:
            - ^/api(?!/doc(?:\.json)?$)
